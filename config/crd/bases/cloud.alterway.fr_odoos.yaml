---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: odoos.cloud.alterway.fr
spec:
  group: cloud.alterway.fr
  names:
    kind: Odoo
    listKind: OdooList
    plural: odoos
    singular: odoo
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Available')].reason
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Odoo is the Schema for the odoos API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OdooSpec defines the desired state of Odoo
            properties:
              database:
                description: |-
                  Database holds the connection details for the database.
                  If this is not provided, a managed PostgreSQL instance will be deployed.
                properties:
                  host:
                    type: string
                  password:
                    type: string
                  postgresVersion:
                    description: |-
                      PostgresVersion specifies the desired PostgreSQL version when a managed database is used.
                      Defaults to "16" if not specified.
                    type: string
                  user:
                    type: string
                type: object
              databaseSecretName:
                description: |-
                  DatabaseSecretName is the name of the secret containing the database credentials.
                  If in managed mode and this field is not provided, a default secret will be created.
                  The secret must contain the keys: 'user', 'password', and 'dbname'.
                type: string
              enterprise:
                description: Enterprise defines the configuration for Odoo Enterprise
                  edition.
                properties:
                  enabled:
                    description: |-
                      Enabled enables the Odoo Enterprise edition.
                      If set to true, the operator will download the Enterprise addons.
                    type: boolean
                  repositoryURL:
                    description: |-
                      RepositoryURL is the Git URL of the Enterprise repository.
                      Defaults to "git@github.com:odoo/enterprise.git" if not specified.
                    type: string
                  sshKeySecretRef:
                    description: |-
                      SSHKeySecretRef is the name of the secret containing the SSH private key
                      for authentication with the git repository.
                      The key must be stored under the key 'ssh-privatekey'.
                    type: string
                  version:
                    description: |-
                      Version is the git reference (branch, tag, or commit) to checkout.
                      If not specified, it defaults to the Odoo version (Spec.Version).
                    type: string
                type: object
              ingress:
                description: Ingress defines the desired state for the Ingress resource.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations is a map of string keys and values to
                      add to the Ingress metadata.
                    type: object
                  enabled:
                    description: Enabled specifies if an Ingress resource should be
                      created.
                    type: boolean
                  host:
                    description: |-
                      Host is the hostname to be used for the Ingress rule.
                      Required if Ingress is enabled.
                    type: string
                  ingressClassName:
                    description: |-
                      IngressClassName is the name of the IngressClass to use.
                      Defaults to "nginx" if not specified.
                    type: string
                  tls:
                    description: |-
                      TLS specifies if TLS should be enabled for the Ingress.
                      If true, it will use a secret named "<odoo-instance-name>-tls".
                      You are responsible for creating this secret, for example with cert-manager.
                    type: boolean
                type: object
              logs:
                description: Logs defines the logging configuration.
                properties:
                  volumeEnabled:
                    description: |-
                      VolumeEnabled specifies if a persistent volume should be used for logs.
                      If not set or set to true, a PVC will be created.
                      If set to false, Odoo will log to stdout.
                    type: boolean
                type: object
              modules:
                description: Modules defines the Odoo modules configuration.
                properties:
                  install:
                    description: |-
                      Install is a list of Odoo modules to install/update at initialization/upgrade.
                      Example: ["sale", "crm", "account"]
                      These modules must be available in the Odoo image or in configured repositories.
                    items:
                      type: string
                    type: array
                  repositories:
                    description: |-
                      Repositories defines additional Git repositories to clone into the addons volume.
                      Useful for custom or community modules not bundled with the Odoo image.
                      These repositories will be cloned into /mnt/extra-addons/<repository.Name>.
                    items:
                      description: GitRepositorySpec defines details for cloning a
                        Git repository.
                      properties:
                        name:
                          description: Name is the name of the subdirectory within
                            /mnt/extra-addons where the repository will be cloned.
                          type: string
                        sshKeySecretRef:
                          description: |-
                            SSHKeySecretRef is the name of the secret containing the SSH private key
                            for authentication with the git repository.
                            The key must be stored under the key 'ssh-privatekey'.
                            This is only used for SSH URLs.
                          type: string
                        url:
                          description: URL is the Git URL of the repository (e.g.,
                            "https://github.com/my-org/my-addons.git").
                          type: string
                        version:
                          description: |-
                            Version is the git reference (branch, tag, or commit) to checkout.
                            Defaults to "master" or "main" if not specified.
                          type: string
                      required:
                      - name
                      - url
                      type: object
                    type: array
                type: object
              options:
                additionalProperties:
                  type: string
                description: |-
                  Options allows to override any key-value pair in the odoo.conf file.
                  These values will be merged with the defaults.
                type: object
              redis:
                description: Redis defines the configuration for Redis session storage.
                properties:
                  enabled:
                    description: Enabled enables Redis for session storage.
                    type: boolean
                  host:
                    description: |-
                      Host is the hostname of the external Redis server.
                      Required if Managed is false.
                    type: string
                  managed:
                    description: |-
                      Managed determines if the operator should deploy a Redis instance.
                      Defaults to true if Enabled is true and Host is empty.
                    type: boolean
                  port:
                    description: Port is the port of the Redis server. Defaults to
                      6379.
                    format: int32
                    type: integer
                  resources:
                    description: Resources allows setting resource requests and limits
                      for the managed Redis container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  secretRef:
                    description: SecretRef contains the name of the secret with the
                      'password' key.
                    type: string
                type: object
              resources:
                description: Resources allows setting resource requests and limits
                  for the various containers.
                properties:
                  init:
                    description: Init defines resources for the database initialization
                      Job.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  odoo:
                    description: Odoo defines resources for the main Odoo container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  postgres:
                    description: |-
                      Postgres defines resources for the managed Postgres container.
                      This is ignored if using an external database.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              service:
                description: Service defines the desired state for the Service resource.
                properties:
                  type:
                    description: |-
                      Type specifies the type of the service.
                      Defaults to "LoadBalancer".
                    type: string
                type: object
              size:
                description: Size defines the number of Odoo instances
                format: int32
                maximum: 10
                minimum: 1
                type: integer
              storage:
                description: Storage defines the storage configuration for the various
                  persistent volumes.
                properties:
                  addons:
                    description: |-
                      Addons holds the storage configuration for the unified addons volume.
                      This PVC is shared by both custom and enterprise addons using subPaths.
                    properties:
                      accessMode:
                        description: AccessMode for the persistent volume. E.g., "ReadWriteOnce",
                          "ReadWriteMany".
                        type: string
                      size:
                        description: Size of the persistent volume. E.g., "10Gi".
                        type: string
                      storageClassName:
                        description: StorageClassName for the persistent volume.
                        type: string
                    type: object
                  data:
                    description: Data holds the storage configuration for the main
                      Odoo data volume.
                    properties:
                      accessMode:
                        description: AccessMode for the persistent volume. E.g., "ReadWriteOnce",
                          "ReadWriteMany".
                        type: string
                      size:
                        description: Size of the persistent volume. E.g., "10Gi".
                        type: string
                      storageClassName:
                        description: StorageClassName for the persistent volume.
                        type: string
                    type: object
                  logs:
                    description: Logs holds the storage configuration for the Odoo
                      log volume.
                    properties:
                      accessMode:
                        description: AccessMode for the persistent volume. E.g., "ReadWriteOnce",
                          "ReadWriteMany".
                        type: string
                      size:
                        description: Size of the persistent volume. E.g., "10Gi".
                        type: string
                      storageClassName:
                        description: StorageClassName for the persistent volume.
                        type: string
                    type: object
                  postgres:
                    description: Postgres holds the storage configuration for the
                      managed PostgreSQL database volume.
                    properties:
                      accessMode:
                        description: AccessMode for the persistent volume. E.g., "ReadWriteOnce",
                          "ReadWriteMany".
                        type: string
                      size:
                        description: Size of the persistent volume. E.g., "10Gi".
                        type: string
                      storageClassName:
                        description: StorageClassName for the persistent volume.
                        type: string
                    type: object
                type: object
              upgrade:
                description: Upgrade defines the configuration for automatic module
                  upgrades.
                properties:
                  enabled:
                    description: Enabled enables the automatic migration process when
                      the Odoo image version changes.
                    type: boolean
                  modules:
                    description: |-
                      Modules is the list of modules to upgrade (comma-separated).
                      Defaults to "all" if not specified.
                    type: string
                type: object
              version:
                description: |-
                  Version defines the Odoo version to be deployed.
                  This will be used as the tag for the Docker image (e.g., "19", "18.0").
                  Defaults to "19" if not specified.
                type: string
            required:
            - size
            type: object
          status:
            description: OdooStatus defines the observed state of Odoo.
            properties:
              conditions:
                description: conditions represent the current state of the Odoo resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentVersion:
                description: |-
                  CurrentVersion tracks the currently deployed version of Odoo.
                  Used to detect version changes and trigger migrations.
                type: string
              migration:
                description: Migration tracks the status of the module upgrade process.
                properties:
                  completionTime:
                    description: CompletionTime is the time when the migration completed.
                    format: date-time
                    type: string
                  phase:
                    description: Phase indicates the current phase of the migration
                      (e.g., "Pending", "Running", "Succeeded", "Failed").
                    type: string
                  startTime:
                    description: StartTime is the time when the migration started.
                    format: date-time
                    type: string
                type: object
              modulesHash:
                description: |-
                  ModulesHash tracks the hash of the modules configuration (install list + repos).
                  Used to detect changes and trigger updates.
                type: string
              ready:
                description: Ready is a string representation of the ready replicas
                  over the desired replicas, e.g., "1/1".
                type: string
              readyReplicas:
                description: ReadyReplicas is the number of Odoo pods that are ready.
                format: int32
                type: integer
              replicas:
                description: Replicas is the desired number of Odoo pods.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
